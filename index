<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Invitation Message Generator — Demo (Placeholder Data)</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Archivo:wght@500;600;700&family=Inter:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
:root{
  --paper:#EFF1EC;
  --panel:#FFFFFF;
  --ink:#18262A;
  --muted:#6B7770;
  --rule:#D6DBD4;
  --jade:#2F6F62;
  --jade-soft:#E4EEE9;
  --amber:#B0762A;
  --amber-soft:#F7EEDD;
  --flag:#9C4B32;
  --flag-soft:#F6E7E2;
}
*{box-sizing:border-box}
body{
  margin:0;background:var(--paper);color:var(--ink);
  font-family:"Inter",system-ui,sans-serif;font-size:14px;line-height:1.55;
}
.wrap{max-width:1180px;margin:0 auto;padding:36px 20px 80px}

/* header */
.eyebrow{font-family:"IBM Plex Mono",monospace;font-size:10.5px;letter-spacing:.18em;
  text-transform:uppercase;color:var(--jade);margin:0 0 10px}
h1{font-family:"Archivo",sans-serif;font-weight:700;font-size:clamp(24px,3.6vw,34px);
  line-height:1.12;margin:0 0 8px;letter-spacing:-.015em}
.sub{color:var(--muted);margin:0;max-width:66ch}
header{border-bottom:1px solid var(--rule);padding-bottom:22px;margin-bottom:20px}

/* demo banner */
.banner{display:flex;gap:10px;align-items:flex-start;background:var(--amber-soft);
  border:1px solid #E8D6B4;border-radius:6px;padding:11px 14px;margin-bottom:22px;font-size:12.5px}
.banner b{font-weight:600}

/* controls */
.controls{display:flex;flex-wrap:wrap;gap:16px;align-items:flex-end;margin-bottom:18px}
.field{display:flex;flex-direction:column;gap:5px}
label.lbl{font-family:"IBM Plex Mono",monospace;font-size:10px;letter-spacing:.13em;
  text-transform:uppercase;color:var(--muted)}
select,input[type=text]{font-family:"Inter",sans-serif;font-size:13.5px;color:var(--ink);
  background:var(--panel);border:1px solid var(--rule);border-radius:5px;padding:7px 10px}
select:focus,input:focus,button:focus-visible{outline:2px solid var(--jade);outline-offset:1px}
.progress{margin-left:auto;text-align:right}
.progress .num{font-family:"Archivo",sans-serif;font-weight:700;font-size:22px;line-height:1}
.bar{width:170px;height:5px;background:var(--rule);border-radius:3px;margin-top:7px;overflow:hidden}
.bar span{display:block;height:100%;background:var(--jade);width:0;transition:width .3s}

/* flags */
.flags{background:var(--flag-soft);border:1px solid #E7CFC6;border-radius:6px;
  padding:13px 15px;margin-bottom:22px}
.flags h3{font-family:"IBM Plex Mono",monospace;font-size:10.5px;letter-spacing:.14em;
  text-transform:uppercase;color:var(--flag);margin:0 0 8px;font-weight:500}
.flags ul{margin:0;padding-left:17px}
.flags li{margin-bottom:3px;font-size:13px}

/* layout */
.grid{display:grid;grid-template-columns:1.35fr 1fr;gap:22px;align-items:start}
@media(max-width:900px){.grid{grid-template-columns:1fr}}

/* roster */
.card{background:var(--panel);border:1px solid var(--rule);border-radius:8px;overflow:hidden}
.card-head{padding:12px 16px;border-bottom:1px solid var(--rule);display:flex;
  justify-content:space-between;align-items:center}
.card-head h2{font-family:"Archivo",sans-serif;font-size:13px;font-weight:600;margin:0;
  letter-spacing:.01em}
.count{font-family:"IBM Plex Mono",monospace;font-size:11px;color:var(--muted)}

.row{display:grid;grid-template-columns:26px 1fr auto;gap:11px;padding:11px 16px;
  border-bottom:1px solid #EDF0EB;cursor:pointer;align-items:center;
  border-left:3px solid transparent}
.row:last-child{border-bottom:none}
.row:hover{background:#F7F9F6}
.row.active{background:var(--jade-soft);border-left-color:var(--jade)}
.row.sent{opacity:.62}
.tick{width:19px;height:19px;border:1.5px solid var(--rule);border-radius:50%;
  display:grid;place-items:center;font-size:11px;color:transparent;background:#fff}
.row.sent .tick{background:var(--jade);border-color:var(--jade);color:#fff}
.nm{font-weight:500}
.meta{font-family:"IBM Plex Mono",monospace;font-size:11px;color:var(--muted);margin-top:1px}
.meta .warn{color:var(--flag)}
.row-actions{display:flex;gap:6px;align-items:center}
.sal{font-family:"IBM Plex Mono",monospace;font-size:11px;padding:3px 5px;
  border:1px solid var(--rule);border-radius:4px;background:#fff}
button{font-family:"Inter",sans-serif;font-size:12.5px;font-weight:500;cursor:pointer;
  border-radius:5px;border:1px solid var(--rule);background:#fff;color:var(--ink);padding:5px 10px}
button:hover{border-color:var(--jade);color:var(--jade)}
button.primary{background:var(--jade);border-color:var(--jade);color:#fff}
button.primary:hover{background:#265A50}

/* preview */
.preview{position:sticky;top:20px}
.tabs{display:flex;gap:4px;padding:10px 14px 0}
.tab{font-family:"IBM Plex Mono",monospace;font-size:10.5px;letter-spacing:.1em;
  text-transform:uppercase;padding:6px 10px;border:1px solid transparent;border-radius:5px 5px 0 0;
  background:none;color:var(--muted)}
.tab.on{background:var(--jade-soft);color:var(--jade);border-color:var(--rule);border-bottom-color:transparent}
pre{margin:0;padding:16px;font-family:"Inter",sans-serif;font-size:13px;line-height:1.6;
  white-space:pre-wrap;word-wrap:break-word;max-height:430px;overflow:auto}
.tok{background:#FFF0C9;border-radius:3px;padding:0 3px;font-family:"IBM Plex Mono",monospace;
  font-size:11.5px;color:#7A5410}
.pv-foot{padding:11px 14px;border-top:1px solid var(--rule);display:flex;gap:8px;align-items:center}
.hint{font-size:11.5px;color:var(--muted);margin-left:auto;font-family:"IBM Plex Mono",monospace}

footer{margin-top:30px;padding-top:16px;border-top:1px solid var(--rule);
  font-size:12px;color:var(--muted)}
</style>
</head>
<body>
<div class="wrap">

<header>
  <p class="eyebrow">Student organization · Alumni outreach</p>
  <h1>Invitation Message Generator</h1>
  <p class="sub">Merges a contact roster into personalized WhatsApp invitations — one click per recipient, with formatting preserved and send status tracked.</p>
</header>

<div class="banner">
  <span>⚠</span>
  <div><b>Demo version.</b> All names and phone numbers on this page are placeholders. The original roster contains personal contact data and is not shown.</div>
</div>

<div class="controls">
  <div class="field">
    <label class="lbl" for="greet">Time of day</label>
    <select id="greet">
      <option value="pagi">Selamat pagi</option>
      <option value="siang" selected>Selamat siang</option>
      <option value="sore">Selamat sore</option>
      <option value="malam">Selamat malam</option>
    </select>
  </div>
  <div class="field">
    <label class="lbl" for="q">Search roster</label>
    <input type="text" id="q" placeholder="Name or cohort…" style="width:200px">
  </div>
  <div class="progress">
    <div class="num"><span id="sc">0</span> / 17</div>
    <div class="lbl" style="margin-top:3px">Messages sent</div>
    <div class="bar"><span id="pb"></span></div>
  </div>
</div>

<div class="flags">
  <h3>Data quality flags — detected automatically</h3>
  <ul>
    <li><b>Duplicate number:</b> Alumni 06 and Alumni 11 share the same phone number.</li>
    <li><b>Missing number:</b> Alumni 09 and Alumni 15 have no phone number on record.</li>
    <li><b>Unverified salutation:</b> 3 entries where the Ci/Ko guess needs manual confirmation.</li>
  </ul>
</div>

<div class="grid">

  <div class="card">
    <div class="card-head">
      <h2>Recipient roster</h2>
      <span class="count" id="shown">17 recipients · 4 cohorts</span>
    </div>
    <div id="list"></div>
  </div>

  <div class="card preview">
    <div class="tabs">
      <button class="tab on" data-view="merged">Merged preview</button>
      <button class="tab" data-view="template">Template</button>
    </div>
    <pre id="pv"></pre>
    <div class="pv-foot">
      <button class="primary" id="copyBtn">Copy message</button>
      <button id="waBtn">Open in WhatsApp</button>
      <span class="hint" id="hint">Blank lines preserved</span>
    </div>
  </div>

</div>

<footer>
  Built for internal use by a student organization. Runs entirely in the browser — no data leaves the page. Message sending remains manual; the tool prepares and tracks, it does not auto-send.
</footer>
</div>

<script>
const TEMPLATE = `Namo Buddhaya 🙏🏼

Selamat {waktu} {sapaan} {nama},

Perkenalkan, saya pengurus KMBD Area Malang Periode XXXVII.

Sehubungan dengan akan diselenggarakannya Pelatihan Eksternal Bakal Calon Ketua Umum KMBD Periode XXXVIII, kami bermaksud mengundang {sapaan} {nama} selaku alumni Periode {periode} untuk hadir sebagai pengamat alumni.

Adapun kegiatan akan dilaksanakan pada:
Hari/Tanggal : Sabtu–Minggu, 29–30 Agustus 2026
Tempat : Vihara Bhakti Pramuka, Bumi Perkemahan Cibubur

Mohon maaf apabila terdapat kata-kata yang kurang berkenan. Atas perhatian dan kesediaan {sapaan}, saya ucapkan terima kasih.

Namo Buddhaya 🙏🏼`;

const data = [
  {n:"Alumni 01", p:"XXXIII", ph:"0812-0000-0001", s:"Ci"},
  {n:"Alumni 02", p:"XXXIII", ph:"0812-0000-0002", s:"Ko"},
  {n:"Alumni 03", p:"XXXIII", ph:"0812-0000-0003", s:"Ci"},
  {n:"Alumni 04", p:"XXXIII", ph:"0812-0000-0004", s:"Ko", flag:"salutation unverified"},
  {n:"Alumni 05", p:"XXXIV", ph:"0812-0000-0005", s:"Ci"},
  {n:"Alumni 06", p:"XXXIV", ph:"0812-0000-0006", s:"Ko", flag:"duplicate number"},
  {n:"Alumni 07", p:"XXXIV", ph:"0812-0000-0007", s:"Ci"},
  {n:"Alumni 08", p:"XXXIV", ph:"0812-0000-0008", s:"Ko", flag:"salutation unverified"},
  {n:"Alumni 09", p:"XXXIV", ph:"", s:"Ci", flag:"no number on record"},
  {n:"Alumni 10", p:"XXXV", ph:"0812-0000-0010", s:"Ko"},
  {n:"Alumni 11", p:"XXXV", ph:"0812-0000-0006", s:"Ci", flag:"duplicate number"},
  {n:"Alumni 12", p:"XXXV", ph:"0812-0000-0012", s:"Ko"},
  {n:"Alumni 13", p:"XXXV", ph:"0812-0000-0013", s:"Ci"},
  {n:"Alumni 14", p:"XXXVI", ph:"0812-0000-0014", s:"Ko", flag:"salutation unverified"},
  {n:"Alumni 15", p:"XXXVI", ph:"", s:"Ci", flag:"no number on record"},
  {n:"Alumni 16", p:"XXXVI", ph:"0812-0000-0016", s:"Ko"},
  {n:"Alumni 17", p:"XXXVI", ph:"0812-0000-0017", s:"Ci"},
];

let active = 0, view = "merged";
const $ = id => document.getElementById(id);

function build(i){
  const d = data[i];
  return TEMPLATE
    .replaceAll("{waktu}", $("greet").value)
    .replaceAll("{sapaan}", d.s)
    .replaceAll("{nama}", d.n)
    .replaceAll("{periode}", d.p);
}

function esc(s){return s.replace(/&/g,"&amp;").replace(/</g,"&lt;");}

function renderPreview(){
  if(view === "template"){
    $("pv").innerHTML = esc(TEMPLATE)
      .replace(/\{(\w+)\}/g, '<span class="tok">{$1}</span>');
    $("hint").textContent = "4 merge fields";
  } else {
    $("pv").textContent = build(active);
    $("hint").textContent = "Blank lines preserved";
  }
}

function renderList(){
  const q = $("q").value.trim().toLowerCase();
  const html = data.map((d,i)=>{
    if(q && !(d.n+" "+d.p).toLowerCase().includes(q)) return "";
    const meta = d.ph
      ? d.ph + (d.flag ? ` · <span class="warn">${d.flag}</span>` : "")
      : `<span class="warn">${d.flag}</span>`;
    return `<div class="row ${i===active?'active':''} ${d.sent?'sent':''}" data-i="${i}">
      <div class="tick">✓</div>
      <div><div class="nm">${d.n}</div><div class="meta">Periode ${d.p} · ${meta}</div></div>
      <div class="row-actions">
        <select class="sal" data-s="${i}">
          <option ${d.s==='Ci'?'selected':''}>Ci</option>
          <option ${d.s==='Ko'?'selected':''}>Ko</option>
        </select>
        <button data-copy="${i}">Copy</button>
      </div>
    </div>`;
  }).join("");
  $("list").innerHTML = html;
  const vis = data.filter(d=>!q || (d.n+" "+d.p).toLowerCase().includes(q)).length;
  $("shown").textContent = vis + " recipients · 4 cohorts";
}

function renderProgress(){
  const s = data.filter(d=>d.sent).length;
  $("sc").textContent = s;
  $("pb").style.width = (s/data.length*100) + "%";
}

function copyText(t){
  const ta = document.createElement("textarea");
  ta.value = t; ta.style.position="fixed"; ta.style.opacity="0";
  document.body.appendChild(ta); ta.select();
  try{ document.execCommand("copy"); }catch(e){}
  document.body.removeChild(ta);
}

$("list").addEventListener("click", e=>{
  const cp = e.target.closest("[data-copy]");
  if(cp){
    const i = +cp.dataset.copy;
    copyText(build(i));
    data[i].sent = true;
    active = i;
    cp.textContent = "Copied";
    setTimeout(()=>{ renderList(); }, 700);
    renderProgress(); renderPreview();
    return;
  }
  if(e.target.closest(".sal")) return;
  const row = e.target.closest(".row");
  if(row){ active = +row.dataset.i; renderList(); renderPreview(); }
});

$("list").addEventListener("change", e=>{
  const sel = e.target.closest("[data-s]");
  if(sel){ data[+sel.dataset.s].s = sel.value; renderPreview(); }
});

$("greet").addEventListener("change", renderPreview);
$("q").addEventListener("input", renderList);

document.querySelectorAll(".tab").forEach(t=>{
  t.addEventListener("click", ()=>{
    document.querySelectorAll(".tab").forEach(x=>x.classList.remove("on"));
    t.classList.add("on"); view = t.dataset.view; renderPreview();
  });
});

$("copyBtn").addEventListener("click", ()=>{
  copyText(build(active));
  data[active].sent = true;
  $("copyBtn").textContent = "Copied";
  setTimeout(()=>$("copyBtn").textContent="Copy message", 1200);
  renderList(); renderProgress();
});

$("waBtn").addEventListener("click", ()=>{
  $("hint").textContent = data[active].ph
    ? "Demo — would open wa.me/62" + data[active].ph.replace(/\D/g,"").slice(1)
    : "No number on record for this recipient";
  setTimeout(renderPreview, 2500);
});

renderList(); renderPreview(); renderProgress();
</script>
</body>
</html>
